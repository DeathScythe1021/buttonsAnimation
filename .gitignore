# --- 必要的忽略項目 ---

# 1. 依賴套件 (最重要！這個資料夾非常大，不需要上傳)
node_modules/

# 2. 系統暫存檔 (Mac 產生的無用檔案)
.DS_Store

# 3. 編輯器設定 (VS Code 的本地設定，通常不需要分享)
.vscode/

# --- Tailwind 相關 ---

# 4. 產出的 CSS 檔案
# 因為這是電腦算出來的結果，我們只上傳原始碼 (input.css)
# output.css 可以隨時重新產生，所以不用上傳
output.css

# 5. 環境變數 (如果有用到，保護金鑰用)
.env