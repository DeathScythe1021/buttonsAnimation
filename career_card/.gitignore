# 依賴套件 (最重要！絕對不要上傳這個)
node_modules/

# Mac 系統產生的檔案 (避免團隊合作時出現垃圾檔)
.DS_Store

# Windows 系統產生的檔案
Thumbs.db

# Log 紀錄檔
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 環境變數 (雖然你現在沒看到，但未來如果有 .env 放金鑰，一定要忽略)
.env

# 如果你有用 Tailwind build 出來的檔案 (通常在 dist 資料夾，視你的專案需求而定)
# /dist